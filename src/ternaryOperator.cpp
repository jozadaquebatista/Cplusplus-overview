#include <iostream>

/* To compile it use: g++ -o name_your_file name_file.cpp -std=c++11 */

int main()
{

  // EXAMPLE = variable = (condition) ? true : false;
  
  int largestNum = (5>2) ? 5 : 2;
  
  std::cout << largestNum << std::endl;

  return(0);

}